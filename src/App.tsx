import "./App.css";

export default function App() {
  return (
    <main className="app">
      <h1>KOBE in Your Pocket — 管理画面</h1>
      <p>React + Vite + TypeScript 環境はセットアップ済みです。</p>
      <p className="hint">
        開発: <code>pnpm dev</code>（/api は Vite プロキシ経由で Backend へ転送）
      </p>
    </main>
  );
}
