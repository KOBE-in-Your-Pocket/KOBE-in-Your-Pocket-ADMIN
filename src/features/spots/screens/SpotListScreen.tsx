import { ScreenPlaceholder } from "../../../layouts";

/** スポット一覧。UI 本体は #21 で実装する。 */
export function SpotListScreen() {
  return (
    <ScreenPlaceholder
      title="スポット"
      note="スポット一覧は #21 で実装します。"
    />
  );
}
