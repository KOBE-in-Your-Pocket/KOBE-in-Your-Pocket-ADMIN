import { ScreenPlaceholder } from "../../../layouts";

/** ユーザー一覧。UI 本体は #22 で実装する。 */
export function UserListScreen() {
  return (
    <ScreenPlaceholder
      title="ユーザー"
      note="ユーザー一覧は #22 で実装します。"
    />
  );
}
